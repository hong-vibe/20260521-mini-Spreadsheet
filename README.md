# 🏓 PingPong Spreadsheet

구글 스프레드시트의 세련된 사용자 경험(UX)과 감각적인 사무 자동화 디자인을 계승함과 동시에, 업무 중 몰래 뇌를 식힐 수 있는 **'오피스 스텔스 카무플라주 게임 모드(테라피 휴식)'**를 탑재한 하이브리드 바닐라 JS 스프레드시트 애플리케이션입니다.

<p align="center">
  <img src="./screenshot_01.png" width="48%" />
  <img src="./screenshot_02.png" width="48%" />
</p>
<p align="center">
  <img src="./screenshot_03.png" width="48%" />
  <img src="./screenshot_04.png" width="48%" />
</p>


- GitHub 원격 저장소: <https://github.com/hong-vibe/20260521-mini-Spreadsheet>

- GitHub Pages 배포 웹 사이트: <https://hong-vibe.github.io/20260521-mini-Spreadsheet/>


## 📝 프로젝트 개요

본 프로젝트는 단순한 표 형식의 데이터 그리드를 넘어, 비즈니스 실무 환경에서 요구되는 강력한 스프레드시트 고유의 UX(동적 열 리사이징, 2차원 드래그 범위 다중 선택, 실시간 로컬스토리지 보존 및 무제한 Undo 히스토리 롤백)를 완벽 구현했습니다. 

이에 더해, 직장인들의 스트레스 해소를 위해 사무실 화면 뒤에 은밀하게 숨겨진 **'5단계 국기 매칭 게임 모듈'**을 결합하여, 실제 업무 중 상사에게 들키지 않고 자연스럽게 머리를 식힐 수 있는 위트 있는 오피스 테라피 환경을 창조해 냈습니다.

---

## ✨ 주요 기능

### 📊 1. 구글 스프레드시트식 고도화 입력 UX 및 리사이징
* **동적 셀 입력 엔진**: `<td>` 내부에 `<input>`을 상시 로드하는 대신, 셀 더블클릭이나 텍스트 입력 시에만 `<input>`을 동적 생성하여 포커싱하고 소멸시키는 고성능 렌더링 UX를 제공합니다.
* **열 너비 동적 조절(Resizing)**: 열 헤더(`<th>`) 우측 경계선에 부드러운 드래그 가이드를 동적 마운트하여, 마우스 드래그로 각 열의 너비를 개별적이고 부드럽게 조정할 수 있습니다.
* **2차원 드래그 다중 범위 선택 & Shift 확장**: 마우스 드래그(`mousedown` ➡️ `mouseenter` ➡️ `mouseup`) 및 `Shift + 클릭` 이벤트를 정밀 계산하여 직관적인 2차원 다중 선택 사각형(`.selected-cell`) 영역을 형성합니다.
* **클립보드 연동**: `Ctrl + C`, `Ctrl + V` 단축키 및 TSV(Tab-Separated Values) 변환 엔진을 마운트하여 외부 엑셀 문서와 데이터의 양방향 연동 복사-붙여넣기를 완벽 지원합니다.

### 💾 2. 로컬스토리지(LocalStorage) 오토 세이브 및 완벽 복구
* **실시간 영속성 보장**: 셀 값의 수정, 삭제 및 행/열의 동적 삽입/삭제 시마다 데이터 스냅샷을 로컬 브라우저 세션에 즉각적으로 자동 보존합니다.
* **초기화 라이프사이클**: 브라우저를 새로고침(F5)하거나 재접속해도 기존에 변경되었던 행/열 개수(`maxRows`, `maxCols`), 개별 열의 너비 및 모든 셀 데이터의 물리적 구조를 선조회하여 완벽 복원합니다.

### ↩️ 3. 초강력 실행 취소(Undo) 메모리 엔진
* **역사 기록 스택**: 최대 50개의 스택 깊이를 가진 메모리 엔진을 결합하여 파괴적인 변경 행위(셀 초기화, 대량 목업 주입, 행/열 삽입 및 삭제 등)가 일어나기 직전의 2차원 원본 스냅샷을 깊은 복사(`saveStateToUndoStack`)하여 기록합니다.
* **단축키 연동**: 화면의 플랫 `되돌리기` 버튼 클릭뿐만 아니라 `Ctrl + Z` 단축키 바인딩을 통해 즉각적인 이전 상태 복구 및 로컬스토리지 자동 저장을 단행합니다.

### ➕ 4. 헤더 호버 연동형 행/열 동적 삽입/삭제 시스템
* **미니멀리즘 플로팅 조절 단추**: 상단 툴바를 어지럽히지 않고, 행(숫자) 및 열(알파벳) 헤더에 마우스를 호버했을 때만 수직(위아래)으로 정렬된 동그란 미니 `+`, `-` 버튼 콤보가 부드럽게 등장합니다.
* **클릭 공간 확보 극대화**: 행/열 전체 선택을 위해 헤더 셀을 클릭할 수 있는 공간을 상상 이상으로 보존하고자 삽입/삭제 버튼을 수직으로 밀착하고 폰트 크기를 12px로 정비하여 뛰어난 마우스 피드백을 실현했습니다.
* **2차원 데이터 시프트 엔진**: 중간 삽입 및 삭제 시 데이터 덮어쓰기가 발생하지 않도록 역순 루프 방식의 데이터 인덱스 이동 엔진을 구현하여 실시간 데이터 유실을 방지하고 그리드를 즉시 재구성(`rebuildGrid`)합니다.

### 🕵️ 5. 5단계 스텔스 위장 및 오피스 카무플라주 게임 모드 (Stealth Therapy Mode)
* **완벽한 위장 인터페이스**: 상단 툴바의 `자유시간` 아이콘을 누르면 기존 셀의 텍스트 데이터 형태를 그대로 유지한 채 A1~D5 구역에 굵은 이중 테두리가 씌워지며 은밀한 2차원 카드 뒤집기 메모리 게임이 작동합니다.
* **국기 이모지 깨짐 완벽 방지**: 윈도우 OS 등에서 국기 이모지가 투박한 영문 약어(CA, KR 등)로 깨져 나오는 현상을 원천 방지하기 위해 국가 코드별 국기 이미지 CDN 리스너를 결합하여 깨끗하고 다채로운 시각 요소를 선사합니다.
* **지연시간 최적화 (0.4초 딜레이 튜닝)**: 카드 반전 매칭 실패 시 불필요하게 사용자의 동작을 묶어두던 구식 0.8초의 딜레이를 **0.4초**로 정밀 단축하여, 빠른 조작에도 오작동이 없으면서 경쾌한 피드백을 느끼도록 조율했습니다.

### 🛠️ 6. 구글 스프레드시트식 플랫 둥근 툴바 및 freetime.png 연동
* **2층형 가상 오피스 레이아웃**: 최상단에 얇고 모던한 가짜 메뉴바(파일, 수정, 보기, 삽입)를 은은하게 얹고, 그 아래에 둥근 슬레이트 그레이 톤의 플랫 툴바를 결합하여 압도적인 시각적 몰입감을 연출했습니다.
* **SVG 아이콘 툴바 정렬**: `되돌리기 | 초기화 | 샘플 데이터 | 엑셀형식 다운로드 | 자유시간`의 배치를 단정한 가로 구분선(`|`)으로 분리하여 어떠한 화면 너비에서도 깔끔한 1열 반응형 레이아웃을 보존합니다.
* **freetime.png 원본 비율 연동**: 동그라미나 사각형에 이미지를 우겨넣지 않고, 둥근 사각형 테두리에 contain 형식으로 부드럽게 감싸 마우스 오버 시 `"자유시간 메모리 게임 시작"`, 활성화 상태 시 `"자유시간 메모리 게임 종료"` 툴팁이 직관적으로 동작하도록 라이프사이클을 완성했습니다.
* **모바일 반응형 터치 스위핑 지원**: 화면 폭이 좁은 모바일 및 태블릿 기기에서도 툴바와 가짜 메뉴바의 텍스트가 찌그러지거나 잘리지 않도록 찌러짐 방지(`flex-shrink: 0`) 및 가로 스와이프 조작(`overflow-x: auto`, 스크롤바 숨김 처리) 환경을 완성하여 쾌적한 모바일 반응형 터치 UX를 제공합니다.

---

## 🛠 기술 스택

* **Core**: HTML5, Vanilla JavaScript (ES6+), Vanilla CSS (Custom Properties, CSS Flexbox/Grid, CSS Animation, Google Fonts - Inter)
* **Library**:
  * [SheetJS (xlsx.full.min.js)](https://cdnjs.cloudflare.com/ajax/libs/xlsx/0.18.5/xlsx.full.min.js) (엑셀 데이터 빌드 및 로컬 다운로드 구동)
  * [Flag CDN (Country Flags SVG)](https://flagcdn.com/) (이모지 브로큰 현상 방지용 벡터 국기 자원 매핑)

---

## 📂 프로젝트 구조

```text
.
├── index.html                  # 2층형 가상 오피스 툴바 레이아웃 및 엑셀 시트 뼈대 구조
├── style.css                   # 프리미엄 파스텔 톤 색상 시스템, 반응형 플랫 툴바 및 그리드 애니메이션
├── script.js                   # ES6 모듈 기반 화면 렌더링 및 이벤트를 조율하는 코어 컨트롤러
├── SpreadsheetUtils.js         # [NEW] 2차원 데이터 엑셀 내보내기 및 TSV 파싱 전담 유틸 모듈
├── GameEngine.js               # [NEW] 5단계 스텔스 위장 카드 매칭 게임의 상태 및 타이머 비동기 엔진
├── docs/
│   ├── audit_report.md         # 특정 함수 집중 방지 및 XLSX 의존성 누수 검증 정적 감사 보고서
│   └── history/                # 단계별 구현 계획서(Implementation Plan) 및 워크스루 히스토리
│       ├── step3_implementation_plan.md
│       ├── step3_walkthrough.md
│       ├── step4_implementation_plan.md
│       └── step4_walkthrough.md
├── prompt.md                   # 전체 개발 이력 및 요구사항 프롬프트 기록부
├── AI_IDE_Spreadsheet_Report.md # AI IDE 연동 종합 개발 보고서
├── freetime.png                # 테라피 게임 기동용 사각형 리소스 이미지
└── README.md                   # 프로젝트 대표 명세서 (본 파일)
```

---

## 🔄 리팩토링 및 아키텍처 개선 사항

### 1단계 리팩토링: 기능 분리 및 가독성 개선 (De-duplication)
* **대단위 함수 격리**: 스파게티 형태로 늘어져 있던 중복 셀 조작 로직을 `updateCellValue`, `deleteCellValue` 등으로 나누어 단일 책임 원칙(SRP)을 확립했습니다.
* **경고 가이드 및 예외 처리**: 샘플 데이터 덮어쓰기 시 `confirm` 가드를 마련하고, 엑셀 익스포트 시 발생할 수 있는 파일 시스템 에러에 대해 `try-catch` 안전장치를 덧대어 앱의 견고성을 극대화했습니다.

### 2단계 리팩토링: 클래스 기반 OOP & 단일 상태 흐름 설계
* **SpreadsheetApp 캡슐화**: 전역 스코프 오염 및 변수 충돌을 완전히 피할 수 있도록 모든 멤버 속성과 메서드들을 단일 거대 클래스인 `SpreadsheetApp` 내부로 격리시켰습니다.
* **Undo 스택 & LocalStorage 완전 동기화**: 격자 크기 변경 및 행/열 수동 조절값까지 포함하는 종합 상태 스냅샷을 딥카피하여 저장함으로써 오토 세이브와 실행 취소 시스템이 유기적으로 공조하도록 결합했습니다.

### 3단계 리팩토링: ES6 Modules 물리적 해체분리 및 되돌리기(Undo) 엔진 고도화
* **ES6 Modules (import/export) 조립형 체제 수립**: 2,200줄에 달하던 방대하고 뚱뚱한 단일 `script.js` 독재 구조를 **3개의 정밀 분산 부품 시스템**으로 전격 해체·독립시켜 단일 파일 코드 압박을 50% 이상 극적으로 줄였습니다.
* **SpreadsheetUtils.js (도구함) 신설**: 엑셀 데이터 파일 다운로드, 클립보드 복사용 TSV 번역기, 행/열 문자-인덱스 숫자 간 정밀 변환 등 웹화면 DOM(HTML 상자)을 만지지 않는 순수한 데이터 가공 수학 유틸리티 부품을 완벽하게 격리 캡슐화하였습니다.
* **GameEngine.js (비밀 게임 뇌) 신설**: 5단계 스텔스 국기 매치 카무플라주 게임의 비동기 타이머, 짝 검사 및 평가 심판, Fisher-Yates 무작위 셔플링, 명예의 전당 탑랭킹 데이터 관리를 전담하는 **보이지 않는 게임 엔진 뇌(StealthGameEngine)**를 수립하였습니다.
* **[롤백 버그 완전 보완] 행/열 리사이즈 되돌리기(Undo) 복원 지원**: 단순 셀 문자 데이터만 롤백되던 이전 시간 여행(Undo) 기능의 핵심 한계점을 포착하고, 복사 스냅샷 생성 시 **격자의 물리적 차원 크기(`maxRows`, `maxCols`)**까지 세트로 보존하며 되돌릴 시 화면 그리드 DOM 구조(`rebuildGrid()`) 자체를 과거 형태 그대로 원복 재구성하는 예리한 고도화를 완성했습니다.

---

## 🚀 설치 및 실행 방법

1. 본 저장소의 소스코드를 클론하거나 다운로드합니다.
   ```bash
   git clone https://github.com/your-username/Stealth-Spreadsheet.git
   ```
2. 프로젝트 루트 폴더에 존재하는 `index.html` 파일을 크롬, 엣지, 사파리 등 현대적인 브라우저에서 직접 더블클릭하여 엽니다.
3. 로컬 파일 시스템에서 별도의 개발 서버 구동 없이도 즉각적으로 모든 스프레드시트 연산 및 은밀한 테라피 게임을 즐기실 수 있습니다.
4. (선택사항) 엑셀 내보내기 및 툴바 기능은 로컬 브라우저의 기본 기능과 SheetJS CDN 연동을 바탕으로 인터넷이 연결된 환경에서 더욱 완벽하게 구동됩니다.

---

## 📄 라이선스

본 프로젝트는 교육 및 비즈니스 보조 위장 도구로서 오픈소스로 개발되었으며, 자유롭게 커스텀 및 수정하여 사용하실 수 있습니다.

---

## 🤖 AI 활용 기록

개발 과정에서 제가 실제로 명령했던 주요 질문(프롬프트)들과, 이를 통해 스프레드시트를 고쳐나간 생생한 과정입니다. 어려운 개발 지식을 배제하고 실제 제 기록장 형식으로 정리했습니다.

### 💡 내가 실제로 내린 작업 지시 (프롬프트)

* **[1차 요청] 미니 스프레드시트 기본 뼈대 만들기**
  * *"index.html, style.css, script.js 세 파일로 나누어 미니 스프레드시트 앱을 만들어주세요. key-value 형태의 객체(spreadsheetData)로 셀 데이터와 좌표를 관리하도록 하고, 셀 안에 입력창이 들어간 9x9 크기의 테이블을 생성해주세요. 포커스된 셀 좌표를 알려주는 인디케이터와 엑셀 다운로드용 Export 버튼도 포함해주세요."*
* **[3차 요청] 엔터 입력 시 셀 포커스 아래로 이동 및 복사/붙여넣기 연동**
  * *"엑셀처럼 셀에 값을 입력하고 엔터를 치면 아래 칸으로 커서가 자동으로 이동하는 기능과, 복사한 엑셀 데이터를 이 앱의 같은 위치 셀에 붙여넣기(TSV 클립보드) 할 수 있는 복사/붙여넣기 연동 기능을 구현해주세요."*
* **[7차 요청] Del 키로 셀 지우기, 마우스로 열 너비 조절 및 샘플 데이터 가드 장착**
  * *"셀을 선택하고 Del을 눌러도 값이 삭제되지 않는데 고쳐주세요. 긴 값들이 뒷부분이 안 보이는데, 셀 헤더 사이에 마우스를 올리면 ↔ 아이콘이 나오고 클릭해서 좌우로 드래그하면 열 너비를 조절하는 기능(열 리사이징)을 구현해주세요. 그리고 샘플 데이터 버튼을 만들되 덮어쓰기 경고창을 띄우고 '예'를 누를 때만 목업 데이터가 입력되게 해주세요. 마지막으로 제목은 PingPong Spreadsheet로 어울리는 아이콘과 함께 리브랜딩해 주세요."*
* **[10차 요청] 되돌리기(Undo) 버튼 및 Ctrl + Z 단축키 살리기**
  * *"샘플 데이터 버튼 왼쪽에 프리미엄 스타일의 되돌리기 버튼이 안 나오고, 단축키 Ctrl+Z도 반응하지 않는데 수정해서 제대로 동작하게 완성해 주세요."*
* **[17차/18차 요청] 구글시트식 둥근 툴바 리뉴얼 및 한글 텍스트 명칭 정비**
  * *"버튼 텍스트가 2줄로 겹쳐서 나와 지저분합니다. 구글 스프레드시트 메뉴처럼 간결하게 리뉴얼해주고, 버튼 사이는 | 으로 구분해서 '되돌리기 | 초기화 | 샘플 데이터 | 엑셀형식 다운로드' 형태로 배치해주세요. 또한 엑셀로 내보내기 버튼명을 '엑셀형식 다운로드'로 교체하고, 샘플 데이터 마우스 오버 시 '목업 데이터 샘플 자동 입력' 툴팁을 띄워주세요."*

---

### 📊 AI의 초안 코드를 토대로 실제로 보완해 나간 사항
AI가 제안한 소스코드를 실제 조작해보면서 발생한 불편함과 오작동을 찾아내고, 디테일한 사용성을 위해 다음 부분들을 맞춤형으로 튜닝했습니다.

* **중간 삽입/삭제 시 데이터가 날아가는 현상 해결**:
  * AI가 처음에 짜준 행/열 삽입 삭제 코드는 새 칸이 비집고 들어올 때 기존 내용 위에 덮어씌워져서 공들여 작성한 데이터가 통째로 지워지는 심각한 버그가 있었습니다.
  * 이를 방지하기 위해 새로운 행이나 열이 추가되는 기준 인덱스부터 가장 끝 셀까지 루프를 **역순(거꾸로)**으로 돌면서 데이터를 한 칸씩 영리하게 밀어주는 복사 엔진을 설계하여 데이터가 절대 공중분해되지 않도록 안전하게 해결했습니다.
* **마우스 오작동이 없도록 버튼 레이아웃 변경**:
  * 행이나 열 머리글에 나타나는 플러스(+), 마이너스(-) 버튼이 한 줄로 붙어 있다 보니, 행 전체를 선택하려는 원래의 마우스 클릭 영역을 심하게 가리고 원치 않는 삭제 버튼이 자꾸 클릭되는 불편함이 있었습니다.
  * 마우스가 편하게 지나갈 수 있도록 **버튼 두 개를 위아래(수직)로 정렬**하고 글자 크기를 **12px로 고정**하여, 조작 편의성을 획기적으로 개선했습니다.
* **그리드 재구성 시 단축키 및 리스너 중복 버그 방지**:
  * 행이나 열을 중간에 추가해서 화면의 바둑판(테이블 DOM)을 새로 그릴 때마다 기존에 살아있던 마우스 드래그 이벤트와 로컬스토리지 핸들러가 메모리 속에서 중복으로 등록되어 마우스 감도가 튀는 버그를 발견했습니다.
  * 테이블을 리빌드(`rebuildGrid`)할 때마다 기존 이벤트 바인딩을 깨끗이 초기화하고 순차적으로 재바인딩해 주는 생명주기 관리 구조를 정비하여 깨끗하고 매끄러운 입력 상태를 보존해 냈습니다.
* **모바일 반응형 터치 스위핑 대응 및 레이아웃 복원**:
  * 해상도가 좁은 모바일 환경에서 접속 시, 상단 메뉴바와 플랫 툴바의 너비가 쪼그라들면서 텍스트 개행이 겹치거나 4번째 버튼부터 우측 화면 밖으로 잘려서 아예 보이지 않는 조작 불능 치명적 오작동을 발견했습니다.
  * 이를 해결하기 위해 모든 헤더 정보 및 버튼에 `flex-shrink: 0`을 엄격히 지정하여 비율 찌그러짐을 방지하고, 툴바 영역에 손가락으로 가볍게 슬라이드하여 남은 메뉴를 순차적으로 활성화하는 **모바일 터치 가로 스크롤(`overflow-x: auto`) 및 스크롤바 숨김 처리**를 단행하여 완벽한 반응형 사용성을 직접 정비했습니다.

---

## 💬 회고

* **어려웠던 점**
  * **기능 점진적 추가에 따른 전역 상태 통합 및 회귀 버그(Regression) 제어**: 단계별로 다양한 혁신적인 아이디어를 순차적으로 구현하는 과정에서, 각 모듈(드래그 범위 선택, 로컬스토리지 복구, 무제한 Undo 스택, 동적 행/열 시프트 엔진) 간의 이벤트 충돌 및 전역 상태 불일치 현상이 발생하여 이를 정밀하게 동기화하고 예외 처리를 단행하는 통합 설계 과정이 무척 까다로웠습니다.
  * **일관된 시각적 위계와 프리미엄 UI 디자인 시스템 정립**: 구글 스프레드시트 고유의 2층형 둥근 플랫 툴바 레이아웃부터 헤더 수직 조절 단추에 이르기까지, 정밀한 40px/32px 그리드 정렬 및 타이포그래피 일관성을 완성하기 위해 다양한 CSS 구조적 설계와 사용자 반응성에 기반한 다차원적 리팩토링 시도를 반복해야 했습니다.

* **개선해보고 싶은 점**
  1. **사칙연산 및 참조 기반 수식 파싱 엔진 구현**: 개별 셀에 연산 기호와 상대/절대 좌표값을 입력했을 때 동적으로 계산하고 화면을 갱신하는 간단한 런타임 수식 연산 기능 탑재
  2. **비즈니스 핵심 수식 함수(SUM, AVERAGE 등) 지원**: 2차원 범위 내 데이터의 통계적 집계를 자동으로 계산하는 내장 집계 함수 해석기(Formula Parser) 구현
  3. **실시간 다중 사용자 동시 협업 기능**: WebSockets 및 충돌 방지 알고리즘(예: OT 또는 CRDT)을 도입하여 여러 사용자가 하나의 시트에서 데이터를 실시간으로 공동 편집할 수 있는 분산 협업 엔진 마운트
