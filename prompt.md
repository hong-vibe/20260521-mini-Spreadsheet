# 누적 프롬프트 기록부 (prompt.md)

이 문서는 사용자가 프로젝트 수행 중 요청한 모든 프롬프트 지시사항을 시간순으로 축적하여 관리하는 기록부입니다.

---

## 📌 [1차 요청] 프로젝트 생성 및 기본 사양 (2026-05-21)

```text
"D:\new-vibe\20260521-mini-Spreadsheet\PRD-SRD-TRD.md"

[시스템 역할]
당신은 깔끔한 아키텍처와 엄격한 코드 분리 원칙(관심사 분리)을 중요하게 생각하는 시니어 프론트엔드 개발자입니다.

[목표]
'index.html', 'style.css', 'script.js' 세 개의 독립된 파일로 나누어 '미니 스프레드시트 웹앱'을 생성하세요. 코드를 절대로 하나로 합치지 마세요. 이 앱은 그리드 입력, 활성화된 헤더 하이라이트 기능, 그리고 구글 스프레드시트와 호환되는 엑셀 파일 내보내기 기능을 지원해야 합니다.

[핵심 데이터 구조]
'script.js' 내부에서 전체 시트 상태를 단일 플랫 자바스크립트 객체로 관리하세요:
`const spreadsheetData = {};`
- Key: 셀의 좌표 문자열 (예: "A2", "C5")
- Value: 사용자가 입력한 값 문자열
- 만약 해당 셀의 입력값이 지워져서 빈칸이 되면, `spreadsheetData` 객체에서 해당 Key를 완전히 삭제(delete)해야 합니다.

[파일 1: index.html 작성 규칙]
1. 깔끔한 HTML5 기본 구조(Skeleton)를 작성하세요.
2. <head> 태그 안에 'style.css'를 링크하고, 아래 SheetJS CDN 라이브러리를 포함하세요:
   <script src="https://cdnjs.cloudflare.com/ajax/libs/xlsx/0.18.5/xlsx.full.min.js"></script>
3. </body> 닫는 태그 직전에 'script.js'를 링크하세요.
4. 상단 제어 바를 만들고 다음 요소를 포함하세요:
   - 포커스된 셀의 좌표를 보여줄 id가 "current-cell"인 span 또는 div 요소 (기본 텍스트: "Cell: 선택 안 됨")
   - id가 "export-btn"이고 텍스트가 "Export Spreadsheet"인 버튼 요소
5. 행 9개(1~9번)와 열 9개(A~I)를 가진 <table> 그리드를 생성하세요. 각 셀 안에는 편집이 가능한 input 필드가 들어가야 합니다.

[파일 2: style.css 작성 규칙]
1. 깔끔하고 현대적이며 전문적인 스프레드시트 그리드 인터페이스를 디자인하세요.
2. 테이블 테두리가 깔끔한 격자선으로 보이도록 `border-collapse: collapse;`를 반드시 사용하세요.
3. 행/열 헤더 셀(<th>)은 연한 회색 배경(#e0e0e0)과 굵은 중심 정렬 텍스트로 스타일을 지정하세요.
4. 셀 내부의 input 필드는 테두리(border)를 없애고, 셀 너비와 높이를 100% 채우도록 설정하세요.
5. 활성화된 헤더를 표현할 `.active-header`라는 유틸리티 클래스를 만드세요. 이 클래스가 적용되면 헤더의 배경색이 부드러운 하늘색(예: #b3e5fc)으로 변경되어야 합니다.

[파일 3: script.js 작성 규칙]
1. 전역 상태를 관리할 `spreadsheetData` 객체를 초기화하세요.
2. 그리드 셀들에 이벤트 리스너를 추가하여 'focus' 및 'blur'(또는 클릭) 이벤트를 처리하세요:
   - 셀이 포커스되면 해당 셀의 열 알파벳과 행 번호를 추출합니다.
   - `#current-cell` 요소의 텍스트를 "Cell: [열][행]" (예: Cell: C1)으로 업데이트합니다.
   - 해당하는 행 헤더와 열 헤더 요소를 찾아 동적으로 `.active-header` 클래스를 추가하세요.
   - 포커스가 다른 셀로 이동하면, 이전에 하이라이트되었던 헤더에서 `.active-header` 클래스를 제거하세요.
3. 셀에 'input' 이벤트 리스너를 추가하세요:
   - 현재 입력된 값을 캡처하여 `spreadsheetData[좌표]`에 저장합니다.
   - 만약 셀 입력값이 비어있으면, `spreadsheetData`에서 해당 좌표 키를 완전히 제거(delete)하세요.
4. `#export-btn` 버튼에 클릭 이벤트 리스너를 추가하세요:
   - SheetJS(XLSX 객체)를 사용하여 `spreadsheetData` 객체의 Key와 Value를 엑셀 파일의 정확한 셀 위치에 매핑하세요.
   - 파일 이름이 `spreadsheet.xlsx`인 실제 파일의 다운로드를 트리거하세요.
   - 생성된 파일은 구글 스프레드시트에 '가져오기(Import)' 했을 때 셀 위치가 웹 화면과 완벽하게 일치해야 합니다.

[출력 안내 규칙]
- 세 개의 파일 코드를 각각 분리하여 완전히 제공하세요.
- React, Vue 같은 외부 프레임워크나 Tailwind 같은 CSS 라이브러리를 절대 사용하지 마세요. 오직 순수 Vanilla JS와 CSS3만 사용해야 합니다.
- 초보 개발자도 핵심 로직을 쉽게 이해할 수 있도록 각 파일 내부에 친절한 한글 주석을 달아주세요.

구현계획을 세워서 나에게 검증을 받고 코딩을 시작하세요. 
한꺼번에 모두 코딩하지 말고 단계별로 수행하세요
```

---

## 📌 [2차 요청] 1차 구현 결과 확인 및 진행 지시 (2026-05-21)

```text
단계별로 구현하세요
```

---

## 📌 [3차 요청] OOP 리팩토링 및 기능 보완 사양 (2026-05-21)

```text
보완사항 
1. 엑셀처럼 입력 후 엔터를 치면 아래셀로 커서가 자동 이동하는 기능
2. 화살표로 셀을 이동하는 기능
3. 행/열의 헤더를 클릭하면 전체행/전체열이 선택되는 기능

리팩토링
함수 역할의 명확한 분리, 1함수=1역할 
UI 생성, 이벤트 처리, 데이터 관리 기능의 모듈화
상태(State) 중심의 객체지향(OOP) 구조로 리팩토링

수정, 보완 전에 git에 기록을 먼저 하세요
내가 제시한 프롬프트는 prompt.md 파일에 계속 쌓주세요. 
바이브코딩 입문자가 코드를 이해하기 쉽게 주석을 잘 달아주세요.
```

---

## 📌 [4차 요청] 전체 범위 선택 및 클립보드 복사/붙여넣기 연동 (2026-05-21)

```text
1. 제일 왼쪽, 제일 상단의 셀을 클릭하면 9x9 셀들이 모두 선택되게 할 수 있나요? 

2. 행/열의 헤더를 선택하면 전체행/열이 선택되는데 
그 때 ctrl+c 를 눌러서 선택된 셀들의 값을 복사하여 
다른 스프레드시트에 붙여넣기를 하면 동일한 칸에 값들이 붙게 할 수 있나요? 

3. 다른 스프레드시트에서 3x2 혹은 9x9 셀들을 선택해서 ctrl+c 해온 값을 
이 앱의 같은 위치 셀에 붙여넣기를 할 수 있나요? 
```

---

## 📌 [5차 요청] 미니 스프레드시트 고도화 (5단계 순차 개발) (2026-05-23)

```text
아래의 5가지 기능을 순차적으로 1단계씩 완성 후 다음 단계 제작을 하겠습니다. 먼저 구현계획을 세우세요

======================

---

# 🛠️ [작업지시서] 미니 스프레드시트 고도화 (5단계 순차 개발)

### [필독] IDE 공통 지시사항

* 모든 단계는 **기존에 작성된 코드를 깨뜨리지 않고 독립적인 함수나 모듈 형태로 추가**해야 합니다.
* 구조(`index.html`), 표현(`style.css`), 동작(`script.js`)의 3대 분리 원칙을 엄격히 고수하세요.
* 각 단계의 구현이 끝날 때마다 전후 기능에 사이드 이펙트(오류)가 없는지 검증 코드를 포함하세요.

---

## 📌 1단계: 실제 엑셀 방식의 입력 UX 전환

* **목적:** 셀 클릭 시 바로 커서가 깜박이지 않고, '선택 대기 상태'와 '입력 상태'를 분리하여 프로페셔널한 UX를 제공합니다.
* **작업 지시:**
* **HTML:** 기존 `<td>` 내부의 `<input>` 태그를 제거하고, 기본 상태는 순수 텍스트만 보여주는 `<td>텍스트</td>` 형태로 변경합니다.
* **CSS:** 셀이 포커스되었을 때의 테두리 스타일(`.cell-selected { outline: 2px solid #107c41; }`)을 정의합니다. (엑셀 고유의 초록 테두)
* **JS (동작 로직):**
1. 셀을 **단일 클릭**하면 해당 `<td>`에 `.cell-selected` 클래스만 부여하고 키보드 대기 상태로 전환합니다. 이 상태에서 글자를 타이핑하기 시작하면 기존 내용을 지우고 새로운 입력이 시작됩니다.
2. 셀을 **더블 클릭**하거나 대기 상태에서 `Enter`를 누르면, `<td>` 내부에 동적으로 `<input>` 태그를 생성해 집어넣어 '수정 모드'로 진입합니다.
3. 입력 중 `Enter`를 누르면 값이 저장되며 **아래쪽 셀로 포커스가 강제 이동**되도록 `keydown` 이벤트를 제어하세요.

(이하 2~5단계 포함 전체 지시서 본문)
---
[2~5단계의 상세 기획 사양 생략 및 보존]
---
```

---

## 📌 [6차 요청] 1단계 범위 드래그 및 Shift+클릭 다중 선택 (2026-05-23)

```text
작업을 진행하세요. 
작업이 완료되면 항상 내가 기능 검증을 하고 승인을 해야 git 저장 & 프롬프트 업데이트 & 다음 단계로 전환 하세요
```

---

## 📌 [7차 요청] 1단계 3대 보완 수정사항 및 PingPong 리브랜딩 (2026-05-23)

```text
수정사항 
1. 셀을 선택하고 del 눌러도 값이 삭제되지 않습니다. 
2. 긴 값들은 뒷부분이 안보입니다. 셀 헤더 사이에 마우스를 올리면 ↔가 나오고 클릭해서 좌우로 드레그하면 열의 넓이를 조절하는 기능을 구현할 수 있나요? 
3. "선택셀 명칭 버튼" 왼쪽에 Sample 버튼을 만들고 그 버튼을 누르면 위에 생성해준 목업 데이터가 입력되게 하세요

이 앱의 제목을 PingPong Spreadsheet 로 변경하고 
pingpong 에 어울리는 아이콘으로 변경하세요

sample 버튼을 클릭하면 경고창이 뜨고 "목업 데이터가 시트에 입력되고 현재 데이터는 지워집니다. 입력할까요?" 예 / 아니요 버튼 표시 > "예"를 누를 경우만 목업 데이터 입력, "아니요" 누르면 현재 셀 데이터 유지 
```

---

## 📌 [8차 요청] 2단계 로컬스토리지 복구 승인 및 3단계 진입 지시 (2026-05-23)

```text
좋습니다. 2단계 구현하세요
```

---

## 📌 [9차 요청] 3단계 Undo(되돌리기) 시스템 구축 지시 (2026-05-23)

```text
Delete 키 단일 삭제 기능은 1단계에 구현했으므로 
undo 기능을 추가하면 됩니다
```

---

## 📌 [10차 요청] 3단계 기획안 검토 피드백 및 개발 승인 (2026-05-23)

```text
Sample 버튼 왼쪽에 프리미엄 스타일의 Undo ↩️ 버튼이 안나옵니다
그리고 ctrl+z 키도 반응을 하지 않습니다
아, 아직 3단계 승인 명령을 하지 않았군요. 승인합니다. 3단계 진행하세요
```

---

## 📌 [12차 요청] 5단계 테라피 휴식 게임 설계 지시 및 기획 보완 (2026-05-23)

```text
5x4 20개의 셀, 10개국의 국기를 이용하세요. 
그리고 세계의 민폐국가인 미국, 일본, 이스라엘 국기를 제외하고 10개국을 선택하세요
타이머도 띄워서 완료 기록도 일자, 시각, 소요시간을 볼 수 있게 해주세요
```

---

## 📌 [13차 요청] 5단계 최종 완료 시각 포맷 보완 및 최종 승인 (2026-05-23)

```text
완료 시각에서 시:분 까지만 표시하세요
그리고 승인합니다
```

---

## 📌 [14차 요청] 5단계 스텔스 위장 및 오피스 카무플라주 게임 모듈화 (2026-05-23)

```text
기존 셀 구조를 그대로 유지한 상태에서 마치 일하고 있는 것처럼 보이게 A1 부터 D5까지 셀만 굵은 테두리가 그려지고 변화가 되게 하고 싶어요. 
현재 셀 데이터 그대로 보이는데 해당셀들만 게임에 이용되는 거죠
이 방식이 가능한지 먼저 확인하세요
```

---

## 📌 [15차 요청] 윈도우 국기 이모지 깨짐 방지 CDN 및 0.4초 딜레이 튜닝 (2026-05-23)

```text
국기 이모지가 안보이고 CA, KR 이렇게 약자만 나오던데 국기 이미지가 보이게 할 수 있나요? 
그리고 한 셀이 뒤집어지고 있을 때 다른 셀을 클릭하면 아무 반응이 없어서 오류처럼 보입니다. 0.8초? 의 지연시간이 너무 길어요
그리고 승인합니다
```

---

## 📌 [16차 요청] 구글 툴바형 플랫 디자인 리뉴얼 및 freetime.png 버튼 연동 (2026-05-23)

```text
잘했어요, 가능은 잘 구현됩니다 
먼저 깃으로 현재 상태를 기록하세요. 

그리고 디자인 수정합니다. 
1번 이미지처럼 버튼 텍스트가 2줄로 겹쳐서 나와서 디자인이 깔끔하지 않습니다. 
2번 이미지의 구글 스프레드시트의 메뉴처럼 간결하게 메뉴로 구성하고 
테라피 휴식이 아닌 첨부한 경로의 png 이미지만 버튼으로 사용하세요
"D:\new-vibe\20260521-mini-Spreadsheet\freetime.png"
```

---

## 📌 [17차 요청] 툴바 버튼 미작동 버그 수정, 툴팁 문구 커스텀 및 사각형 이미지 복원 (2026-05-23)

```text
1. 게임을 눌러도 아무 반응이 없어요
2. 게임의 png 이미지는 동그라미 안에 가두지 말고 원본 그대로의 비율로 보여주세요, 그리고 마우스 오버시 : "자유시간 메모리 게임 시작"
3. 버튼 아이콘 옆에 설명도 같이 적히게 합니다, 버튼 사이는 | 으로 구분합니다. 
되돌리기 | 초기화 | 샘플 데이터 | 엑셀로 내보내기 | 게임 아이콘 
이렇게 디자인하세요
```

---

## 📌 [18차 요청] 툴바 버튼 한글명 변경 및 특정 버튼 툴팁 보완 (2026-05-23)

```text
수정하세요
1. 엑셀로 내보내기 > 엑셀형식 다운로드
2. 샘플 데이터 마우스 오버시 : 목업 데이터 샘플 자동 입력
```

---

## 📌 [19차 요청] 코드 모듈화 상태 및 XLSX 라이브러리 연동 감사 의뢰 (2026-05-23)

```text
잘 구현됐어요

과제 테스트 및 구조 항목에 아래 체크리스트에 부합하는지 검증하세요

[ ] 특정 함수 집중(단일 파일화) 방지 및 모듈화
[ ] 필수 파이브러리 Import 누락 점검
```

---

## 📌 [20차 요청] README.md 및 보고서, 프롬프트 기록부 전면 업데이트 (2026-05-23)

```text
"D:\new-vibe\20260514-GitHub-Finder\README.md"

1. 위 url의 readme.md 파일을 참고하여 pingpong spreadsheet 앱의 Readme.md 파일을 생성하세요. 

2. AI_IDE_Spreadsheet_Report.md 와 prompt.md 파일도 업데이트 하세요
```

그리고 승인합니다
